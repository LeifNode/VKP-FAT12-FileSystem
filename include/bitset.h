#ifndef bitset_H
#define bitset_H

#include "common.h"

//typedef struct BITSET
//{
//	void* bitset;
//	uint32_t bit_count;
//} BITSET;
//
//BITSET* alloc_bitset(uint32_t bit_count);
//void free_bitset(BITSET* bitset);
//
//bool get_bitset_entry(uint32_t bit, BITSET* bitset);
//void set_bitset_entry(uint32_t bit, bool bit_value, BITSET* bitset);
//
//void clear_bitset(BITSET* bitset);

#endif